module.exports = function(app){

    // var nodemailer  = require('nodemailer');
    // var crypto      = require('crypto');

    // var config = {
    //   mail: require('../config/mail'),
    //   admin_mail_address: require('../config/admin_mail_address'),
    // };

    // // sample_mail_data = {
    // //   from: 'no-reply@zorsay.com',
    // //   to: 'vender007@gmail.com',
    // //   subject: 'Welcome to Zorsay',
    // //   text: 'Thankyou for registering with us. (this could also be html)'
    // // }

    // app.sendmail = function(mail_data, callback) {
    //     console.log("inside unrestricted app.sendmail");

    //     var smtpTransport = nodemailer.createTransport('SMTP', config.mail);
    //     smtpTransport.sendMail(mail_data, callback);
    // }
} // end module.exports